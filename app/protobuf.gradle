protobuf {
    protoc {
        artifact = "com.google.protobuf:protoc:$rootProject.ext.protoBufJavaLiteVersion"
    }

    generateProtoTasks {
        all().each { task ->
            task.builtins {
                java {
                    option 'lite'
                }
            }
        }
    }
}
